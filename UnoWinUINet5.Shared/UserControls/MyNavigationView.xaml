<UserControl
    x:Class="UnoWinUINet5.UserControls.MyNavigationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnoWinUINet5.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="UC">

    <Grid x:Name="MainGrid" BorderThickness="0" Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Border x:Name="MainBorder">
            <ListView  x:Name="MainListView">
                
            </ListView>
        </Border>
        <Border Grid.Row="1" x:Name="DocumentsBorder">
            <ScrollViewer  x:Name="DocumentsListScrollViewer">
                <ListView  x:Name="DocumentsListView">
                </ListView>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
